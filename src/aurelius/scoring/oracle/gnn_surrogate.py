"""GNNQuantumOracle — removed in v10.1 (no ONNX model present).

The GNN surrogate class was removed to reduce architectural surface area.
No ONNX model was available at the default path, and the TOM fallback
provides equivalent functionality.
"""
