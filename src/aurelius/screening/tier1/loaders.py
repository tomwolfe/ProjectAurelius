"""Legacy weight loading utilities — kept for reference only.

This module is no longer used.  Tier 1 now uses the deterministic
RDKit-based ``Filter`` class, so no model weights need to be loaded.
"""
