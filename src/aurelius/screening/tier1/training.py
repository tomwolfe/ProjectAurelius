"""Legacy training functions — kept for reference only.

This module is no longer used.  Tier 1 now uses the deterministic
RDKit-based ``Filter`` class, so no neural network training is required.
"""
